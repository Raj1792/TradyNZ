- [x] Implement live email validation for contact form (client-side).
- [x] Implement live email validation for quote form (client-side).
- [x] Add client-side regex checks + live UI feedback (custom validity) for email fields.
- [x] Ensure server-side validation remains unchanged.
- [x] Verify fields: contact.create (email) and quotes.create (email).
- [x] Updated quote email validation to match the richer login/register-style custom error UI.
- [ ] (Optional) Add graceful fallback if JS disabled (no changes required server-side).



